# 2025-12-01 WebAuthn 등록 오류 분석 및 해결

## 현재 상황

### 작동하는 것
- ✅ Face ID/Touch ID 생체인증 (정상 작동)
- ✅ nice.checkplus.co.kr 팝업 (정상 작동)
- ✅ 팝업 WebView 생성 및 SafeArea 위치 조정

### 문제점
- ❌ WebAuthn 등록 실패: "Value "" is not valid" 오류
- ❌ PHP 에러: `/srv/www/user/_header.php` 라인 14, 18에서 null 배열 접근

## 서버 코드 분석

### begin-registration.php (WebAuthn 옵션 생성)
```php
// 반환하는 형식:
{
    "challenge": "base64url(32bytes)",
    "rp": {
        "name": "sefra",
        "id": "sefra.kr"
    },
    "user": {
        "id": "base64url(random_16bytes)",
        "name": "user_id",
        "displayName": "user_id"
    },
    "pubKeyCredParams": [
        {
            "type": "public-key",
            "alg": -7
        }
    ]
}
```

### finish-registration.php (WebAuthn 검증)
필수 필드 검증 (라인 23):
```php
if (!$mobile || !$password_plain || !$cred || !isset($cred['id']) || !isset($cred['rawId'])) {
    // 에러: 'invalid payload'
}
```

**서버가 기대하는 payload:**
```json
{
    "mobile": "사용자 휴대폰번호",
    "password": "비밀번호",
    "credential": {
        "id": "credential_id",
        "rawId": "base64url(credential_id_binary)"
    }
}
```

저장되는 데이터:
- `credentials` 테이블에 `credential_id` (base64) 저장
- `users` 테이블 비밀번호 업데이트

### begin-login.php (로그인용 WebAuthn 옵션)
```php
{
    "challenge": "base64url(32bytes)",
    "allowCredentials": [{
        "type": "public-key",
        "id": "base64url(credentialId)"
    }],
    "timeout": 60000,
    "userVerification": "preferred",
    "rpId": "sefra.kr"
}
```

## iOS 현재 코드 분석

### JavaScript로 생성되는 데이터
```javascript
var regData = {
    id: 'ios_biometric_[deviceId]',
    rawId: '[base64url encoded]',
    type: 'public-key',
    response: {
        clientDataJSON: '[base64url encoded]',
        attestationObject: '[base64url("ios_biometric_attestation")]'
    }
};

window.onPasskeyRegistered(JSON.stringify(regData));
```

### 문제점
1. **rawId가 정확하지 않음** - credential ID binary가 아니라 임의의 값
2. **attestationObject가 유효하지 않음** - 단순 문자열을 인코딩한 것
3. **payload 전송 구조 불명확** - `/ajax/user/info/`로 어떤 형식으로 전송하는지 확인 필요

## 해결 방향

### 1단계: iOS 코드 분석
- iOS에서 실제로 전송하는 payload 확인
- `console.log('Final payload:', JSON.stringify(payload));` 추가
- 네트워크 탭에서 `/ajax/user/info/` POST 요청 확인

### 2단계: 데이터 형식 검증
- `credential.id` ✅ (credential ID string)
- `credential.rawId` ✅ (base64url encoded)
- `mobile` (필수)
- `password` (필수)

### 3단계: iOS attestationObject 수정
현재는 더미값이지만, 서버가 검증하지 않으므로 형식만 유지하면 됨

### 4단계: PHP null 배열 에러 해결
`_header.php` 라인 14, 18에서 null 체크 추가:
```php
// Before
$value = $_SESSION['user']['id'];

// After
$value = $_SESSION['user']['id'] ?? '';
```

## 빌드 버전 정보

| 빌드 | 변경사항 |
|------|---------|
| 61 | ✅ WebAuthn 작동 |
| 62-67 | ❌ FCM localStorage, Alert 디버그 추가 → 작동 안 함 |
| 68 | ✅ 빌드 61 코드로 롤백 (팝업 기능만 유지) |

## 다음 단계

1. **iOS 코드에서 payload 구조 확인**
   - `/ajax/user/info/`로 전송되는 JSON 형식 로깅
   - 필수 필드 모두 포함되는지 확인

2. **네트워크 요청 분석**
   - Mac Safari 개발자 도구로 POST 요청 확인
   - 응답 상태 및 에러 메시지 확인

3. **PHP null 배열 에러 수정**
   - `_header.php` 수정

4. **WebAuthn 최종 검증**
   - 빌드 68로 다시 테스트

## 파일 위치

- **iOS 코드**: `/SefraiOS/ViewController.swift`
- **서버 코드**:
  - `/webauthn/begin-registration.php`
  - `/webauthn/finish-registration.php` (실제 저장 위치는 `/ajax/user/info/`)
  - `/webauthn/begin-login.php`
- **PHP 에러 파일**: `/srv/www/user/_header.php` (라인 14, 18)

## 최신 커밋

- **빌드 68**: dd320ed - WebAuthn을 빌드 61 상태로 롤백
- **WebAuthn 코드 상태**: 팝업 WebView + SafeArea 유지, 디버그 alert 제거

---

**다음 분석:**
1. iOS payload 형식 확인 (console.log 추가 필요)
2. `/ajax/user/info/` 실제 처리 로직 확인
3. PHP null 에러 해결
